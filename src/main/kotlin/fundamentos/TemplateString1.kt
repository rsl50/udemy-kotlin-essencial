package fundamentos

fun main() {
    val aprovados = listOf("João", "Maria", "Pedro")
    println("O primeiro colocado foi ${aprovados[0]}.") // Concatenação em Kotlin usando Template String
    println("O primeiro colocado foi " + aprovados[0] + ".") // Como concatenar string em Java
}